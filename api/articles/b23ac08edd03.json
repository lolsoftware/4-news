{
  "id": "b23ac08edd03",
  "title": "Python string literals are kinda funny",
  "originalTitle": "Python string literals are kinda funny",
  "source": "Hacker News",
  "category": "Tech",
  "url": "https://sebsite.pw/w/20260806-pystrings.html",
  "publishedAt": "2026-08-06T04:44:54.000Z",
  "content": "<div id=\"readability-page-1\" class=\"page\"><div> <p>pop quiz: which of these lines is valid python, and what's the content of the resulting string? </p><pre><code>r'asdf\\'\nr'asdf\\''</code></pre> <details> <summary>answer</summary> <p>the first line is a syntax error; the second line is valid. the content of the resulting string is <samp>asdf\\'</samp> </p><p>the 'r' prefix makes it a <i>raw string literal</i>, so backslash escapes aren't interpreted in any special way. however, raw string literals are still lexed the same way as regular string literals, so they can't end in a backslash, since the following quote isn't treated as the end of the string, even though the quote isn't actually \"escaped\". </p><p>this was definitely originally done to simplify the implementation, which makes what i'm about to show you a lot funnier. </p></details> <details> <summary>the rest of the blog post</summary> <p>here's a valid f-string: </p><pre><samp>&gt;&gt;&gt; <kbd>f'{'}'}'</kbd>\n'}'</samp></pre> <p>here's another one: </p><pre><samp>&gt;&gt;&gt; <kbd>f'{67#}'</kbd>\n... <kbd>}'</kbd>\n'67'</samp></pre> <p>lexing an f-string requires invoking a full python parser on the expression in the curly braces. this expression can contain quotes, be split into multiple lines, and even contain comments! the expression is only terminated by an unparenthesized and uncommented <kbd>}</kbd>, <kbd>!</kbd>, or <kbd>:</kbd>. </p><p>(the fact that the expression can be terminated by <kbd>:</kbd> means that lambda expressions and assignment expressions must be parenthesized inside of f-strings, which is kinda funny i think:) </p><pre><code>f'{lambda: 67}' # syntax error\nf'{x := 67}' # effectively the same as f'{x}'</code></pre> </details> </div></div>",
  "image": null,
  "readingTimeMin": 1,
  "titleStatus": "skipped"
}